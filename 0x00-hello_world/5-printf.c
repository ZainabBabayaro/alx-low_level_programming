#include <stdio.h>
/**
 * main - Entry point
 * Description: 'printf Used to print output string and variables to screen'
 * Return: Always 0 (Success)
*/
int main(void)
{
puts("with proper grammar, but the outcome is a piece of art, \n");
return (0);
}
