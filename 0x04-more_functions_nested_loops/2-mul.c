#include "main.h"

/**
 * mul - return the times of a and b
 * @a: character to be checked
 * @b: character to be checked
 * Return: times
 */

int mul(int a, int b)

{
	int times = a * b;

	return (times);
}
