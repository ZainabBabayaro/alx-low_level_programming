#include "main.h"

/**
 * _isupper - return 1 if c is upper and 0 if otherwise
 * @c: character to be checked
 * Return: 1 or 0
 */

int mul(int a, int b)

{
	int multiply = a * b;

	_putchar(multiply + '0');
	return (0);
}
